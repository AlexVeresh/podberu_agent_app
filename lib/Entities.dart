import 'package:flutter/cupertino.dart';

class ServiceEntity{
  String title;
  String description;
  String iconPath;
  ServiceEntity({
      @required this.title,
      @required this.iconPath,
      @required this.description
    }
  );
}